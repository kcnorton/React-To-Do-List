import React from 'react';
import ReactDOM from 'react-dom';

import './index.css';
import App from './App';

// capital A for App communicates that it is a react component
// instead of an html tag
ReactDOM.render(<App />, document.getElementById('root'));
